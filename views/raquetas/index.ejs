<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Raquetas</title>
    <!-- Añadir Bootstrap desde la CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEJXjQ6RfMGzXXxSaR6+eRInySH4gL2l9+PQ0So+hD4OH9w1eYpzzlZZkV2P1" crossorigin="anonymous">
</head>
<body class="container py-4">
    <h1>Raquetas Disponibles</h1>

    <!-- Mostrar mensaje si está presente -->
    <% if (message) { %>
        <div class="alert alert-success">
            <%= message %>
        </div>
    <% } %>

    <a href="/raquetas/nueva" class="btn btn-primary mb-3">Crear Raqueta</a>

    <ul id="raquetas-lista" class="list-group">
        <!-- Aquí se cargan las raquetas directamente desde el servidor -->
        <% raquetas.forEach(raqueta => { %>
            <li class="list-group-item d-flex justify-content-between align-items-center">
                <div>
                    <strong>Marca:</strong> <%= raqueta.marca %> 
                    <strong>Modelo:</strong> <%= raqueta.modelo %> 
                    <strong>Precio:</strong> $<%= raqueta.precio %>
                </div>

                <!-- Enlaces de acción -->
                <div>
                    <a href="/raquetas/edit/<%= raqueta._id %>" class="btn btn-warning btn-sm">Editar</a>

                    <!-- Formulario para eliminar raqueta -->
                    <form action="/raquetas/<%= raqueta._id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                    </form>

                    <a href="/raquetas/<%= raqueta._id %>" class="btn btn-info btn-sm">Ver</a>
                </div>
            </li>
        <% }); %>
    </ul>

    <!-- Añadir Bootstrap JS desde la CDN (opcional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pzjw8f+ua7Kw1TIq0FfDOMt0MOmRb43vfu6PffIz0c62u43WQh5LD92Fyf0T8Egz" crossorigin="anonymous"></script>
</body>
</html>